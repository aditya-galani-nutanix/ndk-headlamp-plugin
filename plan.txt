1.
P2 (Snapshot & Replicate) — Manual snapshot first:

Run kubectl get crds | grep dataservices and kubectl get applicationsnapshots -A -o yaml to understand the exact spec/status fields
Build TakeSnapshotDialog.tsx: Material UI dialog with form fields:
Application dropdown (populated from Application CRs in namespace)
Expiry duration (dropdown: 24h, 48h, 72h, 1w, custom)
Snapshot name (auto-generated with timestamp, editable)
Wire the "Create" button to SnapshotApi.post(manifest)
Add a status watcher that polls/watches the created snapshot until status.readyToUse: true

2.
P2 (Snapshot & Replicate) — Merged workflow:

Build SnapshotAndReplicate.tsx: extends the snapshot dialog with:
"Also replicate" checkbox (default on)
Replication target dropdown (populated from ReplicationTargetApi.useList())
Implement the chained workflow:
Create ApplicationSnapshot CR
Watch for status.readyToUse: true
Automatically create ApplicationSnapshotReplication CR referencing the snapshot + selected target
Watch for replication status
Show progress stepper UI: "Taking snapshot... Replicating... Done"
This is the hero demo feature — spend time making the progress UI polished

3.
P2 (Snapshot & Replicate):

Polish the progress stepper UI (animations, clear status messages, error recovery)
Handle edge cases:
What if snapshot creation fails? Show error, don't proceed to replication
What if replication target is unreachable? Show meaningful error
What if user navigates away during the workflow? Resume/show status on return
Add a "Snapshot History" tab on the Application detail view (using registerDetailsViewSection)
